# ND PERFUME — CLIENT ENVIRONMENT (optional)
# The storefront talks to the API via relative '/api' paths, which Vite
# proxies to http://localhost:5000 in development (see vite.config.js).
# Only set VITE_API_URL when deploying the frontend separately from the API.
VITE_API_URL=/api
